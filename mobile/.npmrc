# React 19 is newer than peer dependency declarations of many Expo/RN packages.
# This flag tells npm to ignore peerDep conflicts instead of erroring out.
# Safe to keep here until ecosystem fully catches up to React 19.
legacy-peer-deps=true
